const listItem = require('./src/sidebar/list-item')

// const list = Array.from({length: 5}, (_, idx) => ({
//   title: `Item ${idx+1}`,
//   tooltip: idx*idx
// }))

// const listItems = list.map(e => listItem({
//   onClick: () => document.querySelector('#content').textContent = e.title,
//   title: e.title
// }))

// listItems.forEach(e => document.querySelector('#choose-table').appendChild(e))

// const listItems2 = list.map(e => listItem({
//   onClick: () => document.querySelector('#content').textContent = e.title,
//   title: e.title
// }))

// listItems2.forEach(e => document.querySelector('#tools').appendChild(e))